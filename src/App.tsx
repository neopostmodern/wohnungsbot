import React from 'react';

import styles from './App.module.scss'

const App = () => {
    return <div className={styles.wrapper}>
        <h1>Proof-of-concept for Wohnungsbot</h1>
        Migrating to <code>electron-forge</code> with <code>vite</code>...
    </div>
}

export default App